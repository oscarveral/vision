[project]
name = "vision"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "black>=25.9.0",
    "matplotlib>=3.10.7",
    "numpy>=2.3.3",
    "opencv-python>=4.11.0.86",
    "pytest>=8.4.2",
    "scikit-image>=0.25.2",
    "mypy>=1.14.1",
    "pytest-cov>=6.0.0",
    "ruff>=0.9.2",
]

[tool.ruff.lint]
select = [
    # pycodestyle
    "E",
    # Pyflakes
    "F",
    # pyupgrade
    "UP",
    # flake8-bugbear
    "B",
    # flake8-simplify
    "SIM",
    # isort
    "I",
]

[tool.ruff]
line-length = 120

[tool.setuptools]
package-dir = { "" = "src" }
packages = [ "dgst", "rara" ]
